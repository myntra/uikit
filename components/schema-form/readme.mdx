import SchemaForm from './src/schema-form'

# SchemaForm

<Documenter component={SchemaForm}>

```jsx preview
const [schema] = useState({
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "title": "Name",
      "description": "Enter your full name.",
    },
  },
  "required": [
    "name",
  ],
});
const [value, setValue] = useState();

<SchemaForm schema={schema} value={value} onChange={setValue}>
  <Form.Action type="primary">Save</Form.Action>
</SchemaForm>
```

</Documenter>
